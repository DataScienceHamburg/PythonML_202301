#%%#%% package importimport numpy as npimport pandas as pdimport matplotlib.pyplot as pltimport seaborn as snsfrom sklearn.model_selection import train_test_splitfrom sklearn.linear_model import LinearRegressionfrom sklearn.preprocessing import StandardScalerfrom sklearn.metrics import r2_scoreimport numpy as npimport pandas as pdimport matplotlib.pyplot as pltfrom sklearn.model_selection import train_test_splitfrom sklearn.linear_model import LinearRegression#%% data understanding# source: https://www.kaggle.com/code/vrinja/possum-linear-regression/data# %% data importpossum = pd.read_csv('../data/possum.csv')print(possum)# %% data filterpossum = pd.read_csv('../data/possum.csv')print(possum)# %% Wandlung von Text-Inhalt  zu Zahlen in Tabelle possum_formated = pd.get_dummies(possum)print(possum_formated)#%% ModelingX = np.array(possum_formated.drop('totlngth', axis=1))y = np.array(possum_formated.totlngth).reshape(-1, 1)##%% Einteilung der Daten in Training und Test AbschnitteX_train, X_test, y_train, y_test = train_test_split(X,y,                                   random_state=123,                                   test_size=0.25,                                   shuffle=True)print(X_train)# %%reg = LinearRegression()reg.fit(X_train,y_train)print(X_train)